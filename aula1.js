const nome =  "Emmanuel" //string
const idade = 19 // number
const maiordeidade = true // boolean

const frase = `Meu nome é ${nome}, tenho ${idade} anos`

if (idade >= 18) {
    console.log ( "maior de idade")
} else {
    console.log("Menor de idade")
}

const multiplicacao = (idade * 2) / 2

console.log(multiplicacao)